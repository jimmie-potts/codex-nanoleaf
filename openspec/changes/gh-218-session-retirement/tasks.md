## 1. Generation-aware input

- [x] 1.1 After the Hub contract task review passes, demonstrate failed snapshot 1.1 admission, add the narrow compatibility validator and explicit version request, and verify valid/invalid generation cases and unchanged closed 1.0 fields.
- [x] 1.2 Demonstrate retained manual project/effect state after a missed retirement, then reconcile changed generations and authoritative absence atomically; verify fresh defaults, released assignments, unchanged peers/global preferences and restart behavior.

## 2. Source evidence and documentation

- [x] 2.1 Run the Python suite, browser checks, workflow checks and owning Hub consumer harness against this source; retain exact candidate evidence including healthy-empty and unavailable feeds.
- [x] 2.2 Update shared-input and development guides with version requirements, installation order, rollback limits and separate source/installed acceptance; verify documented commands.

## 3. Delivery gates

- [ ] 3.1 Complete the owning Hub issue's separately authorized installed and visible-device acceptance, synchronize the affected spec and archive after successful current lookups, then obtain final independent reviews and matching CI before eligible delivery.
