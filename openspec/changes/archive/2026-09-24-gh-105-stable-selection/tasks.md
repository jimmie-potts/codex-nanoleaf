## 1. Stable selection ordering

- [x] 1.1 Add a browser check that selects a non-first task, a Line badge, several Lines, and then clears the selection, asserting unchanged row order and a highlighted row. Observe it fail on the selected-first sort.
- [x] 1.2 Remove selection from the inspector sort and observe the focused check and the full browser suite pass, including the reveal-selected path for a hidden selection.
- [x] 1.3 Update the bridge guide and this capability delta, synchronize and archive the change, and run the workflow checks.
