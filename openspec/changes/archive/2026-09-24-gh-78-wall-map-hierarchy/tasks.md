## 1. Options menu and status readout

- [x] 1.1 Add a hierarchy browser check that expects the labelled Options menu, a single-row wall heading, a readout without Line or task totals, and no duplicated hints. Observe it fail on the current page.
- [x] 1.2 Move Show all numbers, orientation and assembly playback into the Options menu, build the live status readout, remove the duplicated hints, compact the task card head and narrow the inspector. Observe the hierarchy check and the updated existing suites pass.

## 2. Scenarios, documentation and delivery

- [x] 2.1 Cover dense and empty data at 390, 800 and 1440 px, keyboard focus, accessible names, reduced motion, disconnected and pending states, and passive menu use in the hierarchy check with screenshots; run the full browser, Python and workflow suites.
- [x] 2.2 Update the bridge guide, synchronize and archive this change, publish the PR with the comparison and evidence. Independent reviews, current-head CI and explicit human UI approval remain merge gates.
