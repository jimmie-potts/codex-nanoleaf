## 1. Contract and transport
- [x] 1.1 Pin released validator/schema/fixtures and prove valid, invalid and privacy-canary cases with the shared fixture corpus.
- [x] 1.2 Implement configured bounded HTTP snapshot transport; prove auth, owner/version validation, response/deadline bounds, redirect rejection and sanitized failures with fake servers.

## 2. Selection and projection
- [x] 2.1 Implement atomic persisted configure/preflight/select/rollback and identity bindings; prove restart, failed preflight, duplicate ingestion prevention, active-comet rejection and preserved legacy/preferences state.
- [x] 2.2 Project semantic state, notices, qualified read evidence and explicit acknowledgment; prove concurrent identities, new-turn policy, project allocation, request identity and no metadata export.

## 3. Worker and inspection
- [x] 3.1 Integrate shared polling with the sole worker and steady stale rendering; prove Work/Quiet/Free, epochs, reservations, reconnect, no expired comets and source-generation races.
- [x] 3.2 Expose pure sanitized CLI/module inspection and package source tooling; prove no read mutations/device requests, native Linux operation and retained Windows forwarding.

## 4. Delivery qualification
- [x] 4.1 Measure isolated Linux consumer overhead and failure isolation with synthetic profiles, retaining receipts and evidence limits.
- [x] 4.2 Update integration/bridge documentation and linked Hub guide inputs; verify commands, generated output and guide checks.
- [x] 4.3 Run Python/browser/workflow checks, synchronize the spec and archive the change before independent fixed-comparison reviews and configured CI.
