## 1. Projection and lookup

- [x] 1.1 Add failing shared metadata tests and reuse local catalog/lookup rules; verify local titles, project assignments, roots, hub/manual precedence and provider isolation.
- [x] 1.2 Refresh cached metadata during shared polling and project it transactionally; verify unchanged-revision updates, restart, source switching, retirement and unchanged lifecycle/effect epochs.

## 2. Presentation and verification

- [x] 2.1 Add common provider/session fallback and document precedence; verify nearby UUIDs and legacy parity in Python and browser checks.
- [x] 2.2 Run full Python, browser and workflow checks and preserve candidate screenshots for required human review.
