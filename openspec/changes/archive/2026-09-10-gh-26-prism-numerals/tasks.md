## 1. Placement and local assets

- [x] 1.1 Add a pure bounded placement helper using final screen geometry and measured label bounds; preserve a failing neighbour-clearance case, then verify actual-layout transforms, deterministic identity and safe failure behavior.
- [x] 1.2 Integrate the helper through an explicit local asset route and existing package inclusion lists; verify MIME, path bounds and foreground packaged serving without new device access.

## 2. Visibility and interaction

- [x] 2.1 Apply stable luminous label placement and minimum click/text dimensions from the actual screen transform; pass all viewport/rotation/flip collision, containment and legibility checks.
- [x] 2.2 Add the off-by-default browser-local show-all preference and independent visibility reasons; verify selection/highlight/hover/focus combinations, touch, persistence and unchanged accessible names.
- [x] 2.3 Retain the 360 raw number-click cases, modifiers, cross-view associations, geometry-refresh identity and assembly interruption; verify passive requests and the explicit Locate control separately.

## 3. Candidate verification

- [x] 3.1 Update the bridge/asset contract and Linux packaging handoff; verify source and installed boundaries and synchronize all affected specification blocks.
- [x] 3.2 Run the full applicable browser, Python and workflow suites, inspect desktop/mobile and high-DPI captures, and retain the exact candidate evidence for independent and human review.
