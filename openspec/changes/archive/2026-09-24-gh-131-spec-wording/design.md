## Context

Wording-only correction of two specifications after the Windows runtime retirement. See proposal.md.

## Goals / Non-Goals

**Goals:** no specification names Windows routing or the tray as a supported path.

**Non-Goals:** no behavior, code or test change.

## Decisions

- Modify the two requirements in place, keeping every scenario. A design document is included only because the schema asks whether one applies; the change has no technical decisions.

## Risks / Trade-offs

None; the covering tests are unchanged.
