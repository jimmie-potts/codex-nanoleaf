## 1. Linux state and map commands

- [x] 1.1 Honor explicit hook state directories and private runtime behavior; demonstrate the failing hook regression before the change and the passing test afterward.
- [ ] 1.2 Add configured map ports and URL-only Linux operation; verify usable URLs, occupied ports, duplicate startup, and retained browser request protections.

## 2. Fresh installation and services

- [ ] 2.1 Add private Linux runtime preparation using existing device, geometry, hook, and controller operations; verify fresh/invalid destinations, token handling, metadata inputs, hook preservation, and copied runtime files with isolated state.
- [ ] 2.2 Provision the private Python environment, existing pinned MCP host, credentials, CLI launcher, and user service units; exercise foreground commands and configuration with fake device transport and inspect unit commands.
- [ ] 2.3 Verify concurrent clients retain the existing worker lock and controller behavior through existing regressions and focused Linux integration checks.

## 3. Documentation and verification

- [ ] 3.1 Document Linux setup, service operation, ownership ADR, retained Windows compatibility, and #55 acceptance handoff; review applicable agent instructions and contracts against the selected Linux ownership.
- [ ] 3.2 Run Python, browser, MCP, and workflow checks with required dependencies and retain actual results outside the candidate commit.
- [ ] 3.3 Synchronize and verify all three capability changes, archive the complete source change, and rerun workflow checks before independent review.
