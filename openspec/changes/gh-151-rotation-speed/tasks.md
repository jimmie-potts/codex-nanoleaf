## 1. Encoder and validation

- [ ] 1.1 Capture legacy payload hashes, demonstrate missing rotation/speed behavior, implement circular phases and faster timing; verify crest angles, cycle lengths, unchanged hashes and all option bounds on both fixtures.
- [ ] 1.2 Update TypeScript validation, discovery fixtures and MCP enums; verify shared fixtures and exact dispatched commands, including non-spatial rejection.

## 2. Delivery evidence

- [ ] 2.1 Update option documentation and synchronize all three deltas; verify archived specifications with workflow checks.
- [ ] 2.2 Run full Python, browser, MCP and workflow checks in disposable state; report source evidence separately from physical acceptance.
